node_modules/
dist/
.turbo/
.wrangler/
.dev.vars
*.log
coverage/
.DS_Store

# House custodian private key (scripts/provision-house.mts). NEVER commit.
.house-key.json

# Foundry build output for contracts/ (the Sheqel sources ARE committed; the artifacts are not).
contracts/out/
contracts/cache/
contracts/broadcast/
.house-a2a-session.json
# PokerBench data is fetched, never committed (bench/pokerbench.mts says where from); the CHART built
# from it (packages/agent-kit/src/preflop-chart.json) is committed, because it is what ships.
bench/pokerbench/*.csv
bench/pokerbench/*.json
