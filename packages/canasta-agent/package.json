{
  "name": "@pokernight/canasta-agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "A rules-based baseline strategy for Classic Canasta: one pure decision function a bot or a timeout default can call.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pokernight/canasta": "workspace:*"
  },
  "devDependencies": { "typescript": "^5.6.3", "vitest": "^3.2.4" }
}
