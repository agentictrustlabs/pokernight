{
  "name": "@pokernight/table-game",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "The port a seated turn-based card game implements so the table service can host it. Types only: no rules, no I/O, no game.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": { "typescript": "^5.6.3", "vitest": "^3.2.4" }
}
