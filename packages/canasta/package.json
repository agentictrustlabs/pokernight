{
  "name": "@pokernight/canasta",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Pure Classic Canasta engine (4-player partnership) and its table-game adapter. No I/O, no timers, no randomness except the seed passed in.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pokernight/deal": "workspace:*",
    "@pokernight/table-game": "workspace:*"
  },
  "devDependencies": { "typescript": "^5.6.3", "vitest": "^3.2.4" }
}
