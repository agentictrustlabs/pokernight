{
  "name": "@pokernight/deal",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Deterministic dealing: a 32-byte seed, its commitment, and a shuffle that is a pure function of it. Shared by every game and by the host that commits the seed.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": { "@noble/hashes": "^1.8.0" },
  "devDependencies": { "typescript": "^5.6.3", "vitest": "^3.2.4" }
}
