{
  "name": "@pokernight/protocol",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Wire protocol for Pokernight: zod schemas for client commands, server messages, and the poker.act A2A skill.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@pokernight/engine": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": { "typescript": "^5.6.3", "vitest": "^3.2.4" }
}
