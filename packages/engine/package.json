{
  "name": "@pokernight/engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Pure, deterministic no-limit Texas Hold'em engine: side pots, hand evaluation, seeded shuffle with commit-reveal. No I/O.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "bench": "node --import tsx ./bench/bench.ts"
  },
  "dependencies": {
    "@noble/hashes": "^1.8.0"
  },
  "devDependencies": {
    "pokersolver": "2.1.4",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
